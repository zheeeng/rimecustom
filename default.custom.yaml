# default.custom.yaml

patch:
  schema_list:
    - schema: wubi86

  ascii_composer/good_old_caps_lock: true
  ascii_composer/switch_key:
    Caps_Lock: noop
    Control_L: noop
    Control_R: noop
    Eisu_toggle: clear
    Shift_L: noop
    Shift_R: noop
